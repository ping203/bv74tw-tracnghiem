@{
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}
@model MVC_CMS.Models.TN_ExamUser
@if (Model != null)
{
    <h2>
        Bù giờ cho thí sinh "@(Model.TN_User.FullName)"
    </h2>   
                         using (Html.BeginForm())
                         {
    @Html.HiddenFor(m => m.ID)
    <div class="form-group">
        <label for="exampleInputPassword1">
            Thời gian bù giờ (phút)</label>
        <input type="text" name="addTime" value=" " class = "form-control" />
    </div>    
    <input type="submit" name="submit" value="Lưu lại" class="btn btn-primary" />
                         
                         }

}
else
{
    <h2>
        Không tìm thấy thí sinh này.</h2>
}
